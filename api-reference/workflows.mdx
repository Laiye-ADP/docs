---
title: "工作流"
description: "了解 ADP 工作流应用的适用场景、创建方式和 OpenAPI 调用流程。"
---

工作流应用用于把文档处理、业务判断、人工审核和结果交付等多个步骤组合成可重复运行的业务流程，适合单个抽取接口无法覆盖的多文档、多步骤或需要条件判断的场景。

<Columns cols={2}>
  <Card title="业务工作流模板" icon="layout-template">
    ADP 提供财务应付、费用报销和金融财报等预置业务流程。你可以查询模板目录，获取稳定公开的 `application_id` 和当前账号的可运行状态。
  </Card>
  <Card title="自定义工作流" icon="messages-square">
    根据你的文档、业务规则和处理目标，在 ADP 官方平台中通过 AI 对话助手创建、调整和发布工作流。
  </Card>
</Columns>

<Warning>
  当前暂不支持通过 OpenAPI 创建自定义工作流。请前往 [ADP 官方平台](https://adp.laiye.com/?utm_source=apidoc)，通过 AI 对话助手描述业务需求并创建工作流；完成测试和发布后，再使用本分组中的 OpenAPI 查询和运行。
</Warning>

## 创建并调用工作流

1. **选择创建方式**：使用已有业务工作流模板，或在 ADP 官方平台中通过 AI 对话助手创建自定义工作流。
2. **测试并发布**：在平台中确认工作流的输入、处理步骤和输出符合预期，然后发布应用。
3. **获取应用 ID**：业务工作流模板使用“查询业务工作流模板”接口返回的稳定公开 `application_id`；自定义工作流使用“查询可运行的工作流应用”接口返回的 `id`。
4. **运行工作流**：根据是否需要等待结果，选择同步运行或创建异步任务；异步运行后可继续查询结果、重新运行或取消运行。

<Info>
  调用业务工作流模板前，请确认模板返回的 `can_run` 为 `true`。如果为 `false`，请先在 ADP 官方平台中添加、配置并发布对应应用。
</Info>

## OpenAPI 能力

| 目标 | 接口 |
| --- | --- |
| 查询业务工作流模板及稳定公开应用 ID | `GET /open/agentic_engine/laiye/v1/app/templates` |
| 查询当前账号可运行的工作流应用 | `GET /open/agentic_engine/laiye/v1/app/applications` |
| 查询工作流应用详情（受限预览） | `GET /open/agentic_engine/laiye/v1/app/applications/{application_id}` |
| 同步运行工作流 | `POST /open/agentic_engine/laiye/v1/app/run` |
| 创建异步运行任务 | `POST /open/agentic_engine/laiye/v1/app/run/create/task` |
| 查询异步运行结果 | `GET /open/agentic_engine/laiye/v1/app/run/query/task/{run_id}` |
| 重新运行或取消运行 | `POST /open/agentic_engine/laiye/v1/app/run/{run_id}/rerun`、`POST /open/agentic_engine/laiye/v1/app/run/{run_id}/cancel` |

OpenAPI 当前可查询业务工作流模板，并为已经创建且发布的工作流提供应用查询、运行和运行管理能力。自定义工作流的创建、调整与发布需要在 ADP 官方平台中完成。
