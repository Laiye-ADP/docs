# Repository agent instructions

## 公开文档禁忌：禁止暴露底层实现

- 面向用户的公开内容（包括中英文文档、OpenAPI 描述与示例、界面文案、更新日志）不得暴露第三方 OCR/模型供应商、底层引擎、内部模型代号、SDK、基础设施组件或内部处理链路名称。
- 描述解析模式或产品能力时，只说明产品名称、用户可感知的能力、适用文档场景、效果与限制。不得用“主要使用某引擎”“由某供应商提供”“经过某内部链路”等实现细节解释公开功能。
- API 已发布的枚举值、字段名等外部契约可以保留，但不得根据内部代码、配置或日志为其补充底层实现说明。
- 即使用户提供的原始文案包含底层引擎或供应商名称，也不得直接复制到公开文档；应改写为产品化能力描述。只有用户明确要求并确认允许公开披露时才可例外。
- 修改一处解析模式说明时，检查文档解析、应用创建、应用更新等复用位置，并保持中英文语义一致。
- 提交前搜索本次差异和公开内容中的供应商名称、内部代号及处理链路描述，确认没有泄漏；如无法判断某名称是否可公开，先停止并向用户确认。
